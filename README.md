# River FIS

## Current status

- Scaffold complete.
- Read-only folder intelligence pipeline works.
- API route contracts are live through the local stdlib server.
- Action plans are dry-run first.
- Executor is guarded by safety and requires explicit execution confirmation.
- GUI polish is next; current black/gold River UI is preserved.

## Quick start

```bash
python scripts/start.py
```

Open `http://127.0.0.1:8450/` for the current River UI. See `docs/RUNBOOK.md` for API, diagnostics, and smoke-flow commands.
